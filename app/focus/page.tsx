import { FocusSession } from "@/components/FocusSession";

export const metadata = {
  title: "Deep Focus — Tracker",
  description: "Timed deep work sessions with sound and desktop alerts.",
};

export default function FocusPage() {
  return <FocusSession />;
}
